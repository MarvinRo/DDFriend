import { StyleSheet } from 'react-native';

export const HomeStyles = StyleSheet.create({
  cards:{
    display:"flex",
    maxHeight:150,
    maxWidth:150,
    backgroundColor:'gray',
    borderRadius:50,
    
  }
});
